// src/app/(admin)/food-diary/layout.tsx

import React from "react";

export default function FoodDiaryLayout({ children }: { children: React.ReactNode }) {
    return (
        <>
            {children}
        </>
    );
}

