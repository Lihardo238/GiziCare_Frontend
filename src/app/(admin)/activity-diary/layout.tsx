// src/app/(admin)/activity-diary/layout.tsx

import React from "react";

export default function ActivityDiaryLayout({ children }: { children: React.ReactNode }) {
    return (
        <>
            {children}
        </>
    );
}

